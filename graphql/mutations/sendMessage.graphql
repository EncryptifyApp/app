mutation SendMessage($toUserId: String!, $content: String!) {
  sendMessage(toUserId: $toUserId, content: $content) {
      id
      content
      createdAt
      sender {
        id
        username
        profileUrl
        publicKey
      }
  }
}