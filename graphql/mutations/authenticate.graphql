mutation Authenticate($licenseKey: String!, $username: String!, $publicKey: String!,$encryptedPrivateKey: String!) {
  authenticate(licenseKey: $licenseKey, username: $username, publicKey: $publicKey,encryptedPrivateKey: $encryptedPrivateKey) {
    error {
      ...FieldErrorFragment
    }
    sessionToken
  }
}