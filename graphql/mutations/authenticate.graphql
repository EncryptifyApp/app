mutation Authenticate($accountNumber: String!, $username: String!, $publicKey: String!,$encryptedPrivateKey: String!) {
  authenticate(accountNumber: $accountNumber, username: $username, publicKey: $publicKey,encryptedPrivateKey: $encryptedPrivateKey) {
    error {
      ...FieldErrorFragment
    }
    sessionToken
  }
}