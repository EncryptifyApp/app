fragment chatFragment on Chat {
    id
    updatedAt
    createdAt
    members {
        id
        username
        profileUrl
        publicKey
    }
    messages {
        id
        content
        createdAt
        sender {
            id
            username
            profileUrl
            publicKey
        }
    }
}