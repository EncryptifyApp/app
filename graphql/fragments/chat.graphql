fragment chatFragment on Chat {
    id
    updatedAt
    createdAt
    members {
        id
        username
        profileUrl
        publicKey
    }
    messages {
        ...messageFragment
    }
}