query FindAccount($licenseKey: String!) {
    findAccount(licenseKey: $licenseKey) {
        error {
            ...FieldErrorFragment
        }
        user {
            id
            username
            publicKey
            encryptedPrivateKey
        }
    }
}