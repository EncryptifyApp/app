query FindAccount($accountNumber: String!) {
    findAccount(accountNumber: $accountNumber) {
        error {
            ...FieldErrorFragment
        }
        user {
            id
            username
            publicKey
            encryptedPrivateKey
        }
    }
}