query Chats {
    chats {
        id
        updatedAt
    members {
        id
        username
        publicKey
    }
    messages {
      id
      content
      createdAt
      sender {
        username
        profileUrl
      }
    }
    }
}