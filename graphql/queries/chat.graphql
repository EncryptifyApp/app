query Chat($id: String!) {
  chat(id: $id) {
    id
    members {
        id
      username
    }
    messages {
      content
      createdAt
      sender {
        id
        username
        profileUrl
      }
    }
  }
}