query Chat($id: String!) {
  chat(id: $id) {
    id
    members {
        id
      username
      profileUrl
      publicKey
    }
    messages {
      content
      createdAt
      sender {
        id
        username
      }
    }
  }
}